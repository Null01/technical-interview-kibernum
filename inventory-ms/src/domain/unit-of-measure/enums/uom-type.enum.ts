/**
 * @author Andres Duran
 * @version 0.1
 * @since 2026-05-20
 */

/** Tipo de unidad de medida — coincide con el enum `uom_type` del DDL de inventory_db. */
export enum UomType {
  MASS   = 'MASS',
  VOLUME = 'VOLUME',
  UNIT   = 'UNIT',
  PACK   = 'PACK',
}
